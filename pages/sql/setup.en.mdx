# Setup

Now let's begin implementing the database in code, working in a MySQL environment:

## Database Creation

```sql
CREATE DATABASE elearning_system;
```

Next, we select it to ensure we're working on it:

```sql
USE elearning_system;
```

Now, we're ready to structure the database according to the specifications written previously.
